// Подборки проектов живут в @forge/shared (общий тип + evaluator с backend).
export { SELECTION_GROUPS, matchesSelection } from "@forge/shared";
export type { SelectionGroup, ProjectSelection } from "@forge/shared";
