import { OutOfScope } from "@/widgets/out-of-scope/out-of-scope";

export const metadata = {
    title: "Оферта · демонстрация",
};

export default function OfferPage() {
    return <OutOfScope title="Публичная оферта" topic="Оферта" />;
}
