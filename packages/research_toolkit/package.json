{
    "name": "@forge/research_toolkit",
    "version": "0.0.0",
    "private": true,
    "description": "Research / harvest tooling for legacy and competitor sources",
    "type": "module",
    "main": "./dist/index.js",
    "types": "./src/index.ts",
    "exports": {
        ".": {
            "types": "./src/index.ts",
            "default": "./dist/index.js"
        }
    },
    "scripts": {
        "build": "tsc -p tsconfig.json",
        "typecheck": "tsc -p tsconfig.json --noEmit",
        "clean": "rm -rf dist"
    }
}
